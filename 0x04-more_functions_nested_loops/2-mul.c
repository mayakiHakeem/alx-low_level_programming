#include "main.h"
#include <unistd.h>

/**
 * mul - multiplied 2 integral arguments
 * @a: integral formal parameter
 * @b: second integral formal parameter
 * Return: integral result of multiplication
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
