#include "main.h"

/**
 * mul - multiply 2 int
 * @a: first param to be multiplied
 * @b: second param to multiply with
 *
 * Description: Multiply @a and @b
 * Return: result
 */
int mul(int a, int b)
{
	return (a * b);
}
