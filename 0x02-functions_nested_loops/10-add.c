#include <unistd.h>
#include "main.h"

/**
 * add - entry
 * description: add 2 integer and return result
 * @x: formal parameter
 * @y: second parameter
 * Return: an integer
 */
int add(int x, int y)
{
	int result;

	result = x + y;

	return (result);
}
