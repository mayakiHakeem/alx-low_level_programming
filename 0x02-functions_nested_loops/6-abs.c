#include <unistd.h>
#include "main.h"

/**
 * _abs - entry point
 * Description: print absolute of number
 * @int: type of formal parameter
 * @n: formal parameter
 * Return: Always 0 Success
 */
int _abs(int);
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}

}
