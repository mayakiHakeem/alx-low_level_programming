#include <stdio.h>
#include <main.h>

/**
 * main - Entry point
 *
 * Description: Check code
 * Return: Always 0 (success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
